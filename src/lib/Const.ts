export namespace Const {
    export const LOG = "log";

    export enum LogLevel {
        LOG = 0,
        ERROR = 1,
        WARN = 2,
    }

    export enum ResponseCode {
        SUCCESS = 0,
        ERROR = 1,
    }
}
